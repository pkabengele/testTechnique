{
  "devDependencies": {
    "cypress": "^12.7.0",
    "multiple-cucumber-html-reporter": "^3.2.0"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:execute": "cypress run --spec cypress/e2e/features/*",
    "cypress:report": "node cucumber-html-reporter.js"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^15.1.4",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cucumber": "^6.0.7",
    "esbuild": "^0.17.10"
  }
}
